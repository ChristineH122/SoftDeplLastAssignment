apiVersion: v1
kind: Service
metadata:
    name: christinehsoftdepllastassignment
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: christinehsoftdepllastassignment